services:
  order-work:
    build: .
    image: order-work:latest
    container_name: order-work-service
    ports:
      - "3880:80"
    volumes:
      - order-work-data:/app/data
    restart: unless-stopped

volumes:
  order-work-data:
